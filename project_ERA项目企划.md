Project: ERA - 次世代LLM百合调教沙盒项目计划书

1. 项目概述 (Project Overview)
核心愿景：打造一款结合“Godot底层算数引擎”与“LLM大语言模型动态演绎”的次世代文字沙盒游戏。
核心体验：玩家作为“上帝视角的观察者/甩手掌柜”，在脱离了繁杂手动操作的前提下，通过简单的自然语言指令管理庞大的百合后宫。游戏抛弃了传统SLG中随规模扩大而引发的操作地狱，重点聚焦于自动化调教推演、无序突发事件、极具肉感的高质量夜伽交互，以及硬核的“破冰与数值对抗”系统。

2. 核心架构：表现与数据解耦 & 终极破限方案 (Prefill Jailbreak)
本项目的核心工程逻辑是【程序算分，LLM造梦】，且为了应对大模型日益严苛的审查，底层架构进行了彻底的极客化重构：
* 放弃原生Tool API：彻底抛弃 OpenAI/Gemini 官方易被道德拦截的 `Function Calling / Tools` 接口，全面转向类似 SillyTavern 的“文本流提取”模式。
* 预填充强制破限 (Prefill Jailbreak)：在 Godot 发送给 LLM 的 `role: 'model'` 尾部，强行注入 `</think>\n<thinking>` 等预填充标识，从物理层面引导 LLM 进入无审查思考状态。
* 动态三模提示词：`LLM_Client.gd` 拥有 A（后台分发）、B（深夜夜伽）、C（元叙事求助）三种截然不同的情境模式。Godot 负责根据情境拼接《世界观设定》、《角色面板》和《系统提示》，确保不同情况下的回复人设和要求绝对精准。
* 正则引擎截获 (Regex Routing)：LLM 按照要求输出被 ````json ```` 或 `[STAT_CHANGE]` 等宏标签包裹的纯文本。Godot 使用强大的正则表达式强行抠出这些黑箱数据执行加减法，实现数据交互的隐秘降维打击。

3. 场景设定与游戏核心循环 (Gameplay Loop)
游戏以“回合制时间切片(Time Slicing)”的方式运转。每天被划分为四个关键锚点：
* 清晨 (任务规划期 / LLM意图解析)：玩家在UI主界面下达自然语言命令。LLM负责解析并打包为标准机器任务发送给引擎。
* 白天 (后台运算期 / Godot单帧推演)：调教师与目标进入【调教室】。Godot在此抛弃API请求，通过后台代码的一帧“暗骰对抗（d100）”，瞬间执行成百上千次的加减法和经验计算。
  * **连续多重判定机制 (Task Sequences)**: 一个回合（如上午）不再是只执行一次单一判定，而是类似于ERA中的多回合指令串。LLM或玩家可以一次性派发“连续7-10组动作”（例如：先进行3次爱抚破冰积累欲望，随后进行4次后庭开发，最后进行1次多重高潮冲刺）。Godot会在本地引擎内瞬间模拟完成这套连招序列。
  * **所见即所得的数值战报 (Transparent Stat Logging)**: 为了保证玩家能明确感知到系统的运作，任何一次指令判定成功后，**后台推演日志必须在当前动作后明确附加“经验与属性的具体变动值”（例如：`[C感觉获得 100 经验]`、`[羞耻心降级至 49]`）**，并在单人回合调教结束后，输出该回合的属性变动总览。绝不能只在后台静默算分。
* 中午 (中断窗口 / 随机事件触发点)：LLM被唤醒并收到早上打包的干涩数据。它判断是否需要插入“突发事件（如自慰被抓）”，并向Godot发回通知供玩家干预。
* 夜晚 (结算与夜伽期 / LLM全量演绎)：Godot投喂全天数据的总结与特质标签。LLM生成极具细节的晚报，玩家可挑选角色进入脱离UI干扰的深度“夜伽”纯文本扮演环节。

4. 角色生态与深度维度系统 (Stats & Ecology)
4.1 角色定位
* 自由性奴：具备自由行动权，日常调剂NPC。
* 爱奴/调教师：拥有高等级技巧与小恶魔属性，负责执行破冰或深渊指令的核心干员。
* 肉便器/教具：失去常规羞耻心，用于拔高新人理论经验上限的辅助肉体。

4.2 基于COC检定的硬核属性系统 (Base Stats)
抛弃简单的线性“积累”，改为“暗骰检定”与“对数扩张经验池”：
* A. 基础主属性 (判定基石)：【百合顺从】。作为所有行动成功率的基底分。
* B. 阻力属性 (减值修正)：【羞耻心】、【反抗/傲娇度】。前期调教的巨大壁垒，产生强大的执行减值。可以通过自然语言对话避免。
* C. 突破力属性 (加值修正)：【欲望】、各部位【感觉 (M/B/A/C/V/P)】。由破冰指令（如舔阴、爱抚）强行积累，当突破力覆盖阻力时，角色身体背叛理智，强制服从指令。
* D. 验收属性 (核心烙印)：【接受度 (Devotion)】。判定主线进程的核心门槛，极其难升。
* 经验对数机制：采用类似网游的高额正反馈。调教成功获得的经验值（Exp）巨大且直观，但属性从N升至N+1所需的经验槽呈现对数级暴增，平滑控制后期游戏寿命。

4.3 终极连击：多重高潮与寸止 (Edging & Multiple Orgasms)
废弃传统手动兑换的“珠子”系统。
* 【日常经验所见即所得】：只要调教动作通过了COC暗骰检定，角色除了增加欲望和百合顺从外，**会立刻获得对应受击部位（如C/V/B感觉）的基础经验**，绝不落空。
* 【寸止技巧/忍耐】：决定了角色能将快感憋在 99% 临界状态的耐力（包含扶她P寸止）。
* 多重高潮机制：多重高潮**不是一个单一指令**，而是一个由连续动作堆叠出的**【效果放大器】**。
  * 过程：在一个回合（如上午）的连招指令中，调教师分别执行了“舔阴”、“揉胸”、“后庭插入”等多次指令。每次指令成功，在获得基础经验的同时，也会积累该部位的“临时快感”。
  * 寸止判定：如果被调教者的【寸止技巧】足够高，她能在这些连续刺激下，将 B、C、A 等多个部位的快感全部强行卡在 99% 的濒临绝顶状态而不立即释放。
  * 引爆结算：随后，调教师下达【允许高潮】指令（或临时快感冲破了角色的寸止阈值上限）。此时引擎检定发现有多个部位同时处于满溢状态，便会触发“多重高潮”的终极结算效果。
* 收益：多重高潮作为额外的乘区暴击！如果触发2重高潮，涉事部位获得2倍额外经验暴击；如果触发4重高潮，则遵循ERA经典设定，提供16倍的逆天暴击收益！并强制击碎高阶角色的心防，大幅增加接受度。这是突破后期对数经验壁垒的核心解法。

---
> ⚠️ 【核心开发范式：从“固定套餐”到“基于属性因子的动态指令生成” (Dynamic Action Matrix)】
> 为了真正实现“表现层与实现层绝对分离”的设计哲学，本系统在最终形态中将彻底抛弃传统ERA底层庞大的“固定指令库”。游戏主程序中绝对不允许出现如“强制口交”这样具象化的函数名。
> 
> **实现原理（LLM即规则引擎）**：
> 1. LLM 扮演“公式发明家”。当玩家要求进行任意自创动作（如“用带有倒刺的触手摩擦”）时，LLM 不是去呼叫底层的一个死指令，而是直接现场“捏”出一个**高度抽象的算式JSON**发给引擎。
> 2. 输出结构示例：
> `{ "action_flavor": "强制口交", "check_stat": "sensory_M", "reward_stat": "sensory_M", "difficulty_modifier": -20 }`
> 3. Godot 引擎退化为“无情的算盘”，不理解文本语义，只执行冷冰冰的剥离计算：提取 `check_stat` 去对抗羞耻心进行投骰；成功则加 `reward_stat` 经验。
> 
> **系统安全防火墙**：
> 为了防止 LLM 幻觉破坏系统，底层必须部署两道绝对拦截：
> 1. **防词汇捏造**：检查 `check_stat` 和 `reward_stat` 是否存在于 `CharacterData.STAT_KEYS` 中，不存在则拒收或兜底为基础属性。
> 2. **防数值爆炸**：所有通过 JSON 传来的基础经验值或修正，必须经过 `clamp(value, min, max)` 封顶裁切（例如单次奖励不得超过 500），严防 LLM 一次性将角色加至满级。

4.5 逆向调教机制：属性的维护与降级 (Reverse Training)
游戏强调“动态平衡”而非一味拉满，因此引入“减经验与降级”机制。
* 当某项属性（如【羞耻心】）跌破玩家心理预期值，或【欲望】过高导致角色变为无聊的发情机器时，玩家可下达逆向调教指令。
* 【教养指令】：如“抄写圣经”、“仪态训练”。成功执行将恢复（增加）【羞耻心】经验，甚至使其重新升级，重塑角色的矜持感。
* 【禁欲指令】：如“贞操带锁定”、“放置Play”。成功执行将扣减【欲望】的经验值，导致欲望降级，迫使角色回归理智。
* 该系统要求底层算法 (SimulationEngine) 必须支持经验值为负数时的降级判定。

4.6 角色独占记忆系统 (Exclusive Memory Array)
为了超越传统酒馆/AI聊天中常出现的“角色间信息泄露(记忆串话)”问题，系统实现了严格的记忆隔离：
* 记忆黑箱：Godot在角色的数据结构中，除了 `Custom Tags` 之外，额外开辟一个专属于该角色的 `exclusive_memories` (独占记忆数组)。
* 记忆注入：在夜伽或特殊事件结算时，如果发生仅有特定角色（如A和B）知道的秘密事件，LLM会在返回的JSON中指定：`{"add_memory": {"target": ["A", "B"], "content": "昨晚主人在更衣室偷偷摸了我的胸"}}`。Godot解析后，将此字符串存入A和B的私有记忆库，C的记忆库则毫无变动。
* 独立加载：当玩家只和C聊天时，Godot发送给LLM的上下文中绝对不包含这条记忆，从而在物理层面彻底杜绝信息泄露。只有当A或B参与交互时，这条私有记忆才会被拼接到Prompt中，产生极强的“属于我们俩的小秘密”的情感羁绊体验。

4.7 无限自定义标签 (Custom Tags)
LLM专属黑箱数据。仅保存文本Array（如：“母乳体质”、“怕黑”）。引擎无视其逻辑，但在打包发给LLM结算时，LLM会依此生成特殊的表现层文本与额外数值修正（如：因为怕黑，地下室调教效果下降），真正实现千人千面。

---

附录：角色完整硬指标 (Base Stats) 键名规范表
为保证Godot引擎底层的检定和经验池（Exp）计算的一致性，系统采用以下标准的字典键名规范：

1. 心理阻力与基石 (Mind & Resistance)
* `devotion` (接受度/屈服)：验收阶级的终极指标。
* `shame` (羞耻心)：行动判定的核心减值阻力。
* `rebellion` (反抗度/傲娇)：显性不配合阻力。
* `yuri_obedience` (百合顺从)：所有指令成功率的基础分。

2. 生理突破与感觉 (Senses & Lust)
当快感加值越过羞耻心减值，指令将被迫成功。
* `lust` (欲望)：发情度与强力指令的渴望值。
* `sensory_M` (M感觉 - 嘴/唇/舌)
* `sensory_B` (B感觉 - 胸部)
* `sensory_A` (A感觉 - 后庭)
* `sensory_C` (C感觉 - 阴蒂)
* `sensory_V` (V感觉 - 小穴)
* `sensory_P` (P感觉 - 扶她肉棒)
* `edging_control` (寸止技巧/高潮管理)：【被调教者核心属性】，决定角色能将快感憋在 99% 的阈值容量，是触发“多重高潮”经验暴击的前提。

3. 特殊体质与癖好 (Fetish & Constitution)
* `exhibitionism` (露出癖)：对抗暴露场景的羞耻心。
* `semen_addiction` (精液/体液中毒)：影响吞咽与接吻索求。

4. 技巧与执行力 (Skills & Mastery) (调教师核心指标)
此部分区分为“核心程序属性”与“LLM参考风味标签”。
* 【程序级调教技巧】：
  * `skill_M` / `skill_B` / `skill_A` / `skill_C` / `skill_V` / `skill_P`：分别对应六大部位的绝对调教能力。作为调教师指导他人时，直接提供该部位判定的硬核成功率加成。

* 【LLM风味标签 (划归至Custom Tags)】：
  诸如“口交技巧”、“骑乘位技巧”、“施虐狂(Sadism)”、“Cosplay技巧”等。此类体位与性格风味可无限拓展，完全并入 `Custom Tags`。引擎不为其编写条件判断，由LLM在收到指令后，自行衡量是否给予该任务额外的加成修正或特殊文本。
